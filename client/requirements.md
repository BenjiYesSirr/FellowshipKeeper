## Packages
date-fns | Excellent time and date manipulation for the 54-minute timers
lucide-react | Icons for the UI

## Notes
The backend provides endpoints for quests under /api/quests.
The schema uses Date objects for startTime and returnedAt, so we must parse the JSON strings to Dates on the frontend.
We are fully overriding the default theme to create a custom "Lord of the Rings" inspired dark mode with golden accents.
